#include <bits/stdc++.h>
#include <fcntl.h>

using namespace std;
int main()
{
    /*_setmode(_fileno(stdout), _O_U16TEXT);
    wcout << L" \u0444+\n"; //ф
    wcout << L" \u03C8+\n"; //ψ
    wcout << L"      a\n";

    string username = "whatever";
    wstring wideusername;
    for (int i = 0; i < username.length(); ++i)
        wideusername += wchar_t(username[i]);

    const wchar_t *your_result = wideusername.c_str();

    wcout << your_result << endl;
    string a = "abc";
    string b = "xyz";
    a += b;
    cout << a;*/

    /*string arr = "+SH+sh+HS+hs";
    string substr1 = arr.substr(0, 6);
    string substr2 = arr.substr(6, 6);
    arr.clear();
    arr = arr + substr2 + substr1;
    cout << arr;*/
    /*string output_csv = "bellstate_.csv";

    string str = to_string(123);
    output_csv.insert(10, to_string(1234));
    cout << output_csv;*/
    cout << "\'";
}